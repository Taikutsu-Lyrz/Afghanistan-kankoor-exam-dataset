name: Update Total Questions

on:
  push:
    branches: [main, master]
    paths:
      - 'Afghanistan-kankoor-questions-dataset/dataset/data/**/*.json'
  schedule:
    - cron: '0 12 * * *'  # Runs daily at 12:00 PM UTC (4:30 PM Afghanistan time)
  workflow_dispatch:

permissions:
  contents: write

jobs:
  update-count:
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v4
      
      - name: Count questions and create table
        run: |
          echo "| Subject | Questions |" > table.txt
          echo "|---------|----------|" >> table.txt
          
          total=0
          
          cd Afghanistan-kankoor-questions-dataset/dataset/data
          
          for dir in */; do
            if [ -d "$dir" ]; then
              folder=${dir%/}
              for jsonfile in "$folder"/*.json; do
                if [ -f "$jsonfile" ]; then
                  count=$(jq 'length' "$jsonfile")
                  subject=$(echo "$folder" | sed 's/_/ /g' | awk '{for(i=1;i<=NF;i++)sub(/./,toupper(substr($i,1,1)),$i)}1')
                  echo "| $subject | $count |" >> ../../../table.txt
                  total=$((total + count))
                fi
              done
            fi
          done
          
          cd ../../..
          echo "| **Total** | **$total** |" >> table.txt
          echo "TOTAL=$total" >> $GITHUB_ENV
      
      - name: Update README
        run: |
          sed -i '/<!-- QUESTION_TABLE -->/,/<!-- END_TABLE -->/!b; /<!-- QUESTION_TABLE -->/!d; r table.txt' README.md
          rm table.txt
      
      - uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "📊 Update: ${{ env.TOTAL }} total questions"
          file_pattern: README.md
